// Spinner.js
import React from 'react';
import './styles/Spinner.css'; // Add some basic styles for the spinner

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="loader"></div>
    </div>
  );
};

export default Spinner;
